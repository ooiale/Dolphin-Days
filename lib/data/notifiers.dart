//ValueNotifier - holds the data
//ValueListeneableBuilder - listens to the data -> don't need setState

import 'package:flutter/material.dart';

ValueNotifier<int> selectedPageNotifier = ValueNotifier(0);
